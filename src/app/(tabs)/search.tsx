import React from "react";
import { Screen } from "@/components/common/Screen";
import { AppText } from "@/components/common/AppText";

export default function SearchScreen() {
  return (
    <Screen>
      <AppText variant="screenTitle">Search</AppText>
    </Screen>
  );
}
